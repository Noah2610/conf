# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

# mod key to super:
set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango:monospace 10

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
# default: bindsym $mod+d exec dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window (move floating window in larger steps than default)
bindsym $mod+Shift+h move left 100 px
bindsym $mod+Shift+j move down 100 px
bindsym $mod+Shift+k move up 100 px
bindsym $mod+Shift+l move right 100 px

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
# default: bindsym $mod+h split h
bindsym $mod+Shift+r split h

# split in vertical orientation
# default: bindsym $mod+d split v
bindsym $mod+Shift+d split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
# default: bindsym $mod+Shift+space floating toggle
bindsym $mod+Shift+p floating toggle

# change focus between tiling / floating windows
# default: bindsym $mod+space focus mode_toggle
bindsym $mod+p focus mode_toggle

# focus the parent container
#bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# NOAH - WORKSPACE NAMES:
set $workspWWW "1 "
#set $workspWWW "1 "
set $workspMain "2 "
set $worksp3 "3 "
set $worksp4 "4"
set $worksp5 "5"
set $worksp6 "6"
set $worksp7 "7"
set $workspMedia "8 "
set $workspMusic "9 "
#PROFILE=acer
##set $workspSocial "10 "
#PROFILE=h77m
set $workspSocial "10 "
#set $workspSocial "10 "
# extra workspace:
set $workspXtra ""

# for default profile:
#PROFILE_START=default
##set $workspWWW "1-www"
##set $workspMain "2-Main"
##set $worksp3 "3"
##set $worksp4 "4"
##set $worksp5 "5"
##set $worksp6 "6"
##set $worksp7 "7"
##set $workspMedia "8-Game"
##set $workspMusic "9-Music"
##set $workspSocial "10-Social"
##set $workspXtra "extra"
#PROFILE_END

# noteworthy icons:
#		slack
#		code


# switch to workspace
bindsym $mod+1 workspace $workspWWW
bindsym $mod+2 workspace $workspMain
bindsym $mod+3 workspace $worksp3
bindsym $mod+4 workspace $worksp4
bindsym $mod+5 workspace $worksp5
bindsym $mod+6 workspace $worksp6
bindsym $mod+7 workspace $worksp7
bindsym $mod+8 workspace $workspMedia
bindsym $mod+9 workspace $workspMusic
bindsym $mod+0 workspace $workspSocial
# extra workspace:
bindsym $mod+m workspace $workspXtra

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $workspWWW
bindsym $mod+Shift+2 move container to workspace $workspMain
bindsym $mod+Shift+3 move container to workspace $worksp3
bindsym $mod+Shift+4 move container to workspace $worksp4
bindsym $mod+Shift+5 move container to workspace $worksp5
bindsym $mod+Shift+6 move container to workspace $worksp6
bindsym $mod+Shift+7 move container to workspace $worksp7
bindsym $mod+Shift+8 move container to workspace $workspMedia
bindsym $mod+Shift+9 move container to workspace $workspMusic
bindsym $mod+Shift+0 move container to workspace $workspSocial
# extra workspace:
bindsym $mod+Shift+m move workspace $workspXtra

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
# default: bindsym $mod+Shift+r restart
bindsym $mod+Shift+s restart
# exit i3 (logs you out of your X session)
# default: bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 5 px or 5 ppt
        bindsym Down resize grow height 5 px or 5 ppt
        bindsym Up resize shrink height 5 px or 5 ppt
        bindsym Right resize grow width 5 px or 5 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"



# NOAH preferences and settings

# set monitor names as variables
# h77m:
#PROFILE_START=h77m
set $internalDisp "DVI-D-0"
set $disp "DVI-D-0"
#PROFILE_END
#set $disp "HDMI-0"
# acer:
#PROFILE_START=acer
##set $internalDisp "eDP1"
##set $disp "DP1"
#PROFILE_END
#set $disp "eDP1"

# ASSIGN WORKSPACES TO MONITORS:
# h77m:
#PROFILE_START=h77m
workspace $workspWWW output $internalDisp
workspace $workspMain output $internalDisp
workspace $worksp3 output $internalDisp
workspace $worksp4 output $internalDisp
workspace $worksp5 output $internalDisp
workspace $worksp6 output $internalDisp
workspace $worksp7 output $internalDisp
workspace $workspMedia output $disp
workspace $workspMusic output $internalDisp
workspace $workspSocial output $internalDisp
workspace $workspXtra output $internalDisp
#PROFILE_END

#workspace $workspXtra output $disp

# acer:
#PROFILE_START=acer
##workspace $workspWWW output $disp
##workspace $workspMain output $disp
##workspace $worksp3 output $disp
##workspace $worksp4 output $disp
##workspace $worksp5 output $disp
##workspace $worksp6 output $disp
##workspace $worksp7 output $disp
##workspace $workspMedia output $disp
##workspace $workspMusic output $internalDisp
##workspace $workspSocial output $internalDisp
##workspace $workspXtra output $internalDisp
#PROFILE_END



# olive-green:
#set $statusline-color					"#2A856E"
# light-blue:
#set $statusline-color						"#3B8ABF"
# light-grey:
set $statusline-color						"#dddddd"

set $bg-color										"#21242B"
#set $active-color							"#2D9C80"
set $active-color								"#404040"
# blue:
#set $active-window-color				"#22374F"
set $active-window-color				"#3C7D25"
set $inactive-window-color			"#274466"
set $window-border-color				"#202020"
set $inactive-bg-color					"#2f343f"
#set $text-color								"#f3f4f5"
set $text-color									"#e3e3e3"
#set $text-window-color					"#ffeeee"
set $text-window-color					"#000000"
set $inactive-text-color				"#676E7D"
set $window-inactive-text-color	"#C2C6CF"
set $urgent-bg-color						"#CF8600"
set $indicator-color						"#3C7D25"
#set $indicator-color						"#1AABAB"

# general font:
font pango: MonoSans 10
#font pango: DejaVu Sans Mono Bold 10



# BAR:
bar {
	id "1-bar"
	status_command i3status -c /home/noah/.config/i3/i3status.conf | /home/noah/.config/i3/wrapper.py
	tray_output primary
	position top
	separator_symbol " "
	#font pango:Bombardier 12
	#font pango:LCD Mono Bold 10
	#font pango:Ubuntu Mono 12
	#font pango:MonoSans Bold 10
	font pango: FontAwesome, DejaVu Sans Mono Bold 10
	#font pango: FontAwesome 10
	#font pango:Square 10

	# acer:
	#PROFILE_START=acer
##	output $internalDisp
##	output $disp
##	tray_output $internalDisp
##	tray_output $disp
	#PROFILE_END

	colors {
		background	$bg-color
		statusline	$statusline-color
		separator		#cccccc

		# colorclass				border						background					text
		focused_workspace		$active-color			$active-color				$text-color
		active_workspace		$bg-color					$bg-color						$text-color
		inactive_workspace	$bg-color					$bg-color						$inactive-text-color
		urgent_workspace		$urgent-bg-color	$bg-color						#ffffff
		binding_mode				#2f343a						#900000							#ffffff
	}

	# default colors
	#colors {
		#background	#000000
		#statusline	#ffffff
		#separator		#666666
#
		## colorclass				border			background		text
		#focused_workspace		#4c7899			#285577				#ffffff
		#active_workspace		#333333			#5f676a				#ffffff
		#inactive_workspace	#333333			#222222				#888888
		#urgent_workspace		#2f343a			#900000				#ffffff
		#binding_mode				#2f343a			#900000				#ffffff
	#}

}

# window colors
#												border								background							text												indicator
client.focused					$window-border-color	$active-window-color		$text-window-color					$indicator-color
client.unfocused				$window-border-color	$bg-color								$window-inactive-text-color	$indicator-color
client.focused_inactive	$window-border-color	$inactive-window-color	$window-inactive-text-color	$indicator-color
client.urgent						$urgent-bg-color			$urgent-bg-color				#ffffff											$indicator-color

# disable window edge borders
hide_edge_borders both


# BACKGROUNDS:
# hl2 masses:
#exec_always feh --bg-max /home/noah/BGs/hl2_masses.jpg
# isaac:
#exec_always feh --bg-max /home/noah/BGs/rebirth.png
#exec_always feh --bg-center /home/noah/BGs/rebirth_lamb_01.png
#PROFILE=h77m
exec_always feh --bg-fill /home/noah/BGs/rebirth_lamb_02.png
# pixel landscape - by Stephan Hövelbrinks:
#exec_always feh --bg-max /home/noah/BGs/pixelLandscape.png
# milo:
#exec_always feh --bg-max /home/noah/BGs/milo.jpg
# warty:
#exec_always feh --bg-fill /home/noah/BGs/warty-dark.png
#exec_always feh --bg-fill /home/noah/BGs/warty-olive.png
# spirited away - train:
#exec_always feh --bg-fill /home/noah/BGs/train-spiritedaway.png
#PROFILE=acer
##exec_always feh --bg-scale /home/noah/BGs/lo-waves_cover.jpg


# KEY BINDINGS:

# dmenu:
#bindsym $mod+d exec dmenu_run -l 10 -b -p "run:" -fn bombardier -i
#bindsym $mod+d exec emenu_run -l 10 -b -p "run:" -fn bombardier -i
#bindsym $mod+d exec emenu_run -i -l 10 -b -p "run:" -fn "Ubuntu Mono:pixelsize=18" -nb "#21242B" -sb "#3C7D25" -nf "#999999" -sf "#222222"
bindsym $mod+d exec /home/noah/.config/i3/emenu_run.sh -i -l 8 -b -p "$(date +%H:%M)" -fn "Ubuntu Mono:Bold:pixelsize=16" -nb "#21242B" -sb "#3C7D25" -nf "#999999" -sf "#222222"

#PROFILE_START=acer
# media keys:
# volume controls:
##bindsym XF86AudioRaiseVolume exec amixer -D pulse sset Master 5%+; exec cvlc --play-and-exit /usr/share/sounds/freedesktop/stereo/audio-volume-change_edited.mp3
##bindsym XF86AudioLowerVolume exec amixer -D pulse sset Master 5%-; exec cvlc --play-and-exit /usr/share/sounds/freedesktop/stereo/audio-volume-change_edited.mp3
##bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute 0 toggle; exec cvlc --play-and-exit /usr/share/sounds/freedesktop/stereo/audio-volume-change_edited.mp3
# screen brightness controls:
##bindsym XF86MonBrightnessUp exec xbacklight -inc 10
##bindsym XF86MonBrightnessDown exec xbacklight -dec 10
# toggle touchpad:
##bindsym XF86TouchpadToggle exec /home/noah/.config/i3/toggletouchpad.sh
#PROFILE_END

# volume controls:
bindsym $mod+Control+plus exec amixer -D pulse sset Master 5%+; exec cvlc --play-and-exit /usr/share/sounds/freedesktop/stereo/audio-volume-change_edited.mp3
bindsym $mod+Control+minus exec amixer -D pulse sset Master 5%-; exec cvlc --play-and-exit /usr/share/sounds/freedesktop/stereo/audio-volume-change_edited.mp3
bindsym $mod+Control+numbersign exec amixer -D pulse sset Master toggle; exec cvlc --play-and-exit /usr/share/sounds/freedesktop/stereo/audio-volume-change_edited.mp3

# lock screen
bindsym $mod+Shift+x exec i3lock --color $bg-color

# cmus-remote key-bindings:
bindsym $mod+Control+Return exec cmus-remote -u
bindsym $mod+Control+k exec cmus-remote -n
bindsym $mod+Control+j exec cmus-remote -r
bindsym $mod+Control+l exec cmus-remote -k +10
bindsym $mod+Control+h exec cmus-remote -k -10
# volume adjustment
bindsym $mod+Control+Shift+plus exec cmus-remote -v +10%
bindsym $mod+Control+Shift+minus exec cmus-remote -v -10%

# exit without dialog:
bindsym $mod+Control+Shift+e exit

# rename workspace (not practical)
#bindsym mod1+r exec i3-input -F 'rename workspace to "%s"' -P 'New name: '

# toggle space/super
#bindsym $mod+Tab exec /home/noah/.config/i3/switchspace.sh
#bindsym $mod+Shift+Tab exec cat /home/noah/.config/i3/spaceState | espeak

# keylogger:
#bindsym $mod+dead_circumflex exec python /home/noah/prgs/py-keylogger/keylogger.py

# screenshots:
bindsym Print exec gnome-screenshot -f /home/noah/Pictures/Screenshots/$(date +%Y-%m-%d_%H:%M:%S).png
bindsym Shift+Print exec gnome-screenshot -af /home/noah/Pictures/Screenshots/$(date +%Y-%m-%d_%H:%M:%S)_area.png
#bindsym Shift+Print exec gnome-screenshot -af /home/noah/Pictures/Screenshots/$(date +%Y-%m-%d_%H:%M:%S).png
#bindsym Shift+Print exec echo test | espeak

# clear custom status bar output:
#bindsym $mod+Control+Shift+c exec echo -n "" > /home/noah/.bar_output
# kill timer.sh, vlc, and clear bar output:
bindsym $mod+Control+Shift+x exec kill $(pgrep timer.sh), exec pkill vlc, exec echo -n "" > /home/noah/.bar_output

# open floating terminal
bindsym $mod+Shift+Return exec gnome-terminal --role FLOAT

# focus the parent container
bindsym $mod+a focus parent
# focus the child container
bindsym $mod+Shift+a focus child

# move mouse to edge of screen
# move to bottom-right:
#bindsym $mod+Shift+y exec xdotool mousemove $( xdpyinfo | awk '/dimensions/{print $2}' | cut -d"x" -f1 ) $( xdpyinfo | awk '/dimensions/{print $2}' | cut -d"x" -f2 )
# move to top-right:
bindsym $mod+Shift+y exec xdotool mousemove $( xdpyinfo | awk '/dimensions/{print $2}' | cut -d"x" -f1 ) 0

# toggle mouse enabled state
bindsym $mod+Control+adiaeresis exec /home/noah/.config/i3/togglemouse.sh

# disable mouse buttons:
#bindsym --whole-window button1 nop
#bindsym --whole-window button2 nop
#bindsym --whole-window button3 nop
#bindsym --whole-window button4 nop
#bindsym --whole-window button5 nop

# middle mouse button toggles window floating mode:
bindsym button2 floating toggle
# right mouse button moves window to scratchpad
bindsym button3 move scratchpad

# scratchpad:
bindsym $mod+Shift+space move scratchpad
bindsym $mod+space scratchpad show

# toggle autoclick.sh (default: mouse1):
bindsym $mod+Control+a exec /home/noah/bin/autoclick



# FORCE OPEN APPLICATIONS IN SPECIFIC WORKSPACE:
#PROFILE_START=h77m
assign [class="Google-chrome"] $workspWWW
assign [window_role="SessionPrompt"] $workspWWW
# assign [class="Firefox"] $workspSocial
#PROFILE_END
assign [class="Firefox"] $workspWWW
assign [class="Opera"] $workspSocial
assign [class="Steam"] $workspSocial
# for steam updater when launching:
assign [title="Steam"] $workspSocial
assign [class="Slack"] $workspSocial
assign [window_role="MUSIC-cmus"] $workspMusic
assign [window_role="MUSIC-alsamixer"] $workspMusic
for_window [window_role="FLOAT"] floating enable
# games:
assign [class="Binding of Isaac: Afterbirth+"] $workspMedia
assign [class="superhexagon.x86_64"] $workspMedia
assign [title="Enter the Gungeon"] $workspMedia
assign [title="Risk of Rain"] $workspMedia
assign [class="csgo_linux64"] $workspMedia
assign [title="HEAVY BULLETS"] $workspMedia
assign [class="RogueLegacy.bin.x86_64"] $workspMedia
assign [title="Thomas Was Alone"] $workspMedia
assign [title="Serious Sam 3 - Linux"] $workspMedia
assign [title="Papers Please"] $workspMedia
assign [class="net-minecraft-launcher-Main"] $workspMedia
# look into making this non-version-specific:
assign [class="Minecraft 1.12"] $workspMedia
assign [title="The Escapists"] $workspMedia
assign [title="Kingdom"] $workspMedia
assign [class="Thief Town"] $workspMedia
assign [title="Shadow Warrior"] $workspMedia


# AUTO START APPLICATIONS:
exec_always xmodmap /home/noah/.Xmodmap
#exec_always xcape -t 250 -e "Super_L=space"
exec_always pkill xcape; xcape -t 250 -e "Super_R=Escape"

# for space/super switching:
exec_always echo 0 > /home/noah/.config/i3/spaceState

#PROFILE=h77m
exec_always /home/noah/.screenlayout/disp-one.sh
#exec_always /home/noah/.screenlayout/disp-both.sh
#PROFILE=acer
##exec_always /home/noah/.screenlayout/set-primary.sh

# set screensaver timeout (current: 60min - 3600sec):
# set monitor timeout (current: 60min - 3600sec):
#PROFILE_START=h77m
exec_always xset s 3600
exec_always xset dpms 0 0 3600
#PROFILE_END
# acer:
# set screensaver timeout (current: 15min - 900sec):
# set monitor timeout (current: 20min - 1200sec):
#PROFILE_START=acer
##exec_always xset s 900
##exec_always xset dpms 0 1800 1200
#PROFILE_END
# and set standby timeout (current: 30min - 1800sec)


exec /home/noah/Projects/shell/tmux-auto-restore/res.sh
exec redshift-gtk
#PROFILE=acer
##exec nm-applet

#PROFILE_START=h77m
# exec google-chrome
exec steam
exec /home/noah/.dropbox-dist/dropboxd
exec /usr/lib/x86_64-linux-gnu/libexec/kdeconnectd
exec indicator-kdeconnect
#PROFILE_END
#PROFILE=acer
##exec indicator-cpufreq
exec firefox
exec opera
# (not reliable after boot up:)
set $startCmusCmd 'tmux new-session -As music\; new-window cmus\; kill-window -at cmus'
# start cmus in tmux session (in music workspace):
exec gnome-terminal --role "MUSIC-cmus" -x bash -c $startCmusCmd
# start alasmixer (in music workspace):
exec gnome-terminal --role "MUSIC-alsamixer" -x bash -c alsamixer
#exec --no-startup-id i3-msg "workspace $workspMusic; exec gnome-terminal -x bash -c tmux\ new-session\ -As\ music\;\ new-window\ cmus\;\ kill-window\ -at\ cmus"		# start cmus in tmux session and move to workspace 9
#exec --no-startup-id i3-msg "workspace $workspMusic; exec gnome-terminal -x bash -c alsamixer"			# start cmus in tmux session and move to workspace 9

exec --no-startup-id i3-msg "workspace $workspMain; exec gnome-terminal"

